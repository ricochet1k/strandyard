# StrandYard Dashboard

SolidJS dashboard for browsing and editing tasks, roles, and templates with live task updates.

## Run

```bash
npm install
npm run dev
```

## Configuration

Set the API base URL if the stream server is not on the default port:

```bash
VITE_API_BASE=http://localhost:8686 npm run dev
```
